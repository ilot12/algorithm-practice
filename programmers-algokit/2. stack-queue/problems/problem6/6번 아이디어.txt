우선 뒤에를 쭉 확인해야 현재 값을 채울 수 있으므로
앞에서부터 도는 것보다 뒤에서 부터 도는 것이 유리함.

뒤에서 부터 돈다고 했을 때
각 순서에서 해야 하는 것

answer벡터의 특정 위치 값을 계산하는 함수
자신과 같은 것이나 자신보다 작은 값이 나올 때까지 진행.
같다면: 같을 때까지 진행한 횟수 + 그 같은 값
작다면: 작은 값이 나올 때까지 진행한 횟수
혹은 끝까지 도달했다면: 끝까지 진행한 횟수

이걸 메인 반복문에서 뒤에서부터 채우면 됨.
마지막 것은 0이어야 하므로, 마지막 하나 전부터 시작하면 됨.